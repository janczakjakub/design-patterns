export default class TailorReceiver {
    public makeDress(name: string): void {
        console.log(`Creating a dress for ${name}.`);
    }

    public makeTShirt(name: string): void {
        console.log(`Creating a t-shirt for ${name}.`);
    }

    public makePants(name: string): void {
        console.log(`Creating pants for ${name}.`);
    }
}
